# M1 offline validation — 2026-09-19

Scope: E01–E09 on `codex/execution-table`. This report does not validate real provider access, real billing, wallet-brand compatibility or public deployment. The previous approved presentation is checkpointed on `main` at `16fff22`.

## Automated evidence

- E00 hosted CI passed at `e53a77a`: [run 35434526612](https://github.com/inshell-art/Agent-Art-signatures.gallery/actions/runs/35434526612).
- Final `OPEN_MINT_TEST_HTTP=1 npm run test:coverage -- --reporter=dot`: **3,605 tests / 121 files**, **95.02% statements, 91.11% branches, 98.03% functions**. Existing thresholds (93/87/97) were not lowered. This includes real loopback HTTP transport, final review fixes and the optional support route.
- `npm run typecheck`, `npm run build` (including all renderer locks), **65 contract tests**, deployment-manifest validation and **9 manifest-role tests** passed.
- Full suite uses mocked paid transports. The isolated HTTP tests need loopback permission, not external provider access.

Coverage includes durable admission/dispatch ordering, write failures and restart, accounting uncertainty, provider timeouts/late responses/body limits, abstention without MBTI fabrication, fixed subject/model checks, credential-free frozen-result reuse, pricing expiry, session/assessment read deadlines, live expiry and unresolved-submission guards. The 16 native-artifact integrations still compare full SVG bytes against the independent Python oracle and validate actual PNG/metadata; only their new execution-hook argument expectation changed.

## Browser evidence

An isolated temporary HTTP fixture served the actual `assessmentPage`, client script, site CSS and local fonts. Its status/session endpoints were mocked. It did not connect to a chain, install a wallet provider, read the user's sessions or expose a POST mutation. Headless Chrome used fresh temporary profiles through the `visual-dom-cdp` verifier; screenshots were inspected, not inferred from unit tests.

| Case | Viewport/theme | Result |
| --- | --- | --- |
| Ready page becomes expired while idle | 390×844 / light | Live phase changed to expired; Return to mint preserved `Alice_Bob` spelling and explained saved-result reuse; no submission |
| Session request never returns | 390×844 / dark | Read deadline reached; visible Check progress recovery; no wallet/provider request or assumed success |
| Submitted hash with expired request | 390×844 / light | Remained submitted; hash wrapped inside viewport; Return to mint stayed hidden; canonical status GET continued |
| Grok abstention | 1280×900 / dark | Explicit abstention, no mint control or result disclosure; bounded diagnostic reference visible |
| Pending assessment becomes blocked | 390×844 / dark | Operator reference retained; optional Request help became visible; static HTTPS URL contained no private code/reference and was not followed |

All five cases had **zero horizontal overflow**. Font loading was checked and screenshots show the approved light/dark typography. Asset requests returned 200; the stalled-session case intentionally left its read unresolved until the browser aborted. Network logs contained **GET only**, with no external or paid calls. The support URL was a temporary fixture setting, not a configured application contact. These checks are not the full E13/E14 wallet/mobile/accessibility matrix.

## Review findings resolved

- Operational storage errors were previously classifiable as provider failures. Typed persistence errors now retain private causes while reports say `storage`.
- Pricing expiry was initially only a preflight warning. It now disables admission and each paid leg, including an enabled process crossing the boundary, while leaving saved results usable.
- An old model environment setting could block credential-free restart. Model override compatibility is now enforced only for effective paid generation.
- Session recovery feedback and terminal diagnostic retention are covered by client regressions; obsolete connection failures must not remain after a successful retry.

## Live gate

`npm run open:preflight` found both `XAI_API_KEY` and `OPEN_MINT_X_BEARER_TOKEN` absent, generation disabled and no real record files. It performs no network operations or writes. Follow [the one-attempt runbook](../grok-pilot.md) after explicit approval. Its proposed $1 reservation/exposure threshold is **not** a provider-enforced charge ceiling. No live-pilot success or observed cost is claimed.
