# E10 live pilot — stopped at X billing gate

Tested application revision: `e68a22add9b463858ded43557c110a8499664d95` on `codex/execution-table`. Documentation-only changes and an ignored local launcher were present. Profile: `sg-grok-mbti-2026-09-19-v1`; approved canonical target: `karpathy`.

The user approved one X username lookup followed by at most one Grok request, no retries, a $1 application reservation without a guaranteed provider bill cap, and separate user wallet approval for the Anvil mint. Runtime: `.local/open-mint/grok-pilot-20260920/`, app `http://127.0.0.1:3020`, isolated RPC `http://127.0.0.1:18560`, chain ID `31337`.

## Observed result

- Safe attempt reference: `466046e4-6569-4167-a63d-4e20100c8a8e`.
- Admitted at `2026-09-20T07:18:10.525Z`.
- X username lookup: one request; HTTP **402**, `http-error`, receipt duration **662 ms**.
- No verified X identity; no Grok dispatch marker or receipt. The xAI key and pinned request profile remain untested against the live xAI service.
- Outcome: `failed-after-response`; failure category `identity`; reconciliation `operator-review`; automatic retry prohibited.
- No accepted assessment, artwork or mint transaction was recorded. The artifact lifecycle remains `pending` because preparation never started.
- Private store contains one attempt, one failed request, one X receipt and two budget records. Inspection made no provider or RPC requests.

## Billing interpretation and limits

HTTP 402 means Payment Required and points to the X API account's billing/credit gate. This is not an Anvil failure or a Grok inference failure. The bounded receipt stores the status but not the provider's raw error body, so it does not establish the exact balance, app entitlement or problem subtype. Check the X Developer Console account/app associated with the configured bearer token; xAI billing is separate.

[X pricing and credits](https://docs.x.com/x-api/getting-started/pricing), checked September 20, explains that depleted/negative credits and reached spending limits block API requests. [X usage and billing](https://docs.x.com/x-api/fundamentals/post-cap) says failed requests are not billed. This supports an expectation of no charge for the rejection, not an account-specific reconciliation result. The stored receipt correctly retains `unknown` cost, and the $1 reservation remains unresolved rather than being reported as an actual $1 charge or a measured zero.

## Recovery gate

Preserve this namespace, attempt, receipts and budget guards. Do not retry, remove guards, create a replacement namespace, fall back to fixture data or bypass authoritative X identity verification. Resolve the X account issue and confirm billing evidence first. Another paid attempt requires explicit user approval and an audited recovery path linked to this original attempt. Recovery was not implemented at the time of the failure; the subsequent offline increment is recorded below.

E10 is **blocked, not complete**. Real Grok assessment, accepted-result reuse, user-approved mint, reveal and live provenance validation remain unverified. No credits were purchased and no billing settings were changed by the agent.

## User follow-up — September 20, 07:40 UTC

The user reports completing the purchase and having X API credits. This is user-provided funding confirmation, not an independently checked account balance or a reconciled charge for the original request. Read-only inspection confirms the original HTTP 402 attempt and guards are unchanged; no Grok dispatch or additional provider call occurred. A recovery implementation and explicit approval for one additional attempt remain necessary before another paid request.

## Offline implementation follow-up — September 20

The user's standing development instruction authorizes implementing the recovery path and independent plan work without repeated approval questions. It does not extend the previously bounded one-attempt/no-retry paid envelope. The [narrow recovery implementation](../assessment-operations.md#narrow-offline-recovery--e21-increment-brought-forward-for-e10) and its offline tests are now available. No command was applied to this real namespace; the application was not restarted, no additional X/Grok call was made, and no wallet/chain state was changed. Billing evidence and approval for the additional paid attempt remain external gates. The existing running process still serves its previously started code until deliberately restarted.

## Approved recovery staged — September 20, 08:12 UTC

The user subsequently replied **“Approve”** to one additional X lookup for `karpathy`, followed by at most one Grok request, no automatic retries, under the same $1 application reservation/exposure envelope (not a guaranteed bill cap). This satisfies the additional-attempt approval; it does not authorize a third attempt or replace browser wallet approval.

The operator reviewed the signed-in X account's current-cycle billing summary covering the original rejection and recorded a separate zero-cost reconciliation with a private evidence reference. This is operator reconciliation against the visible account summary, at its displayed precision, not a fabricated per-request provider receipt or a guarantee against later provider adjustments. The original X receipt remains `unknown`. Private billing evidence, the exact recovery command and backups remain ignored/local.

Using implementation `a1e366ef00a6d99f35d1ad679d77c88db5bbd526` ([passing CI](https://github.com/inshell-art/Agent-Art-signatures.gallery/actions/runs/35498500637)), read-only review succeeded and the exact reviewed digest was applied under the exclusive writer lock after graceful shutdown and backup. Linked attempt **`a9443f9f-ae30-428d-a9a8-5a5031a5f861`** is staged with no phases and no receipts. Original attempt/receipt bytes and saved deployment were compared byte-for-byte against the backup and are unchanged; the original budget reservation is retained.

The app restarted on the same origin/ports/namespace and restored its Anvil state. Read-only checks verified chain 31337, unchanged contract code and the exact pre-restart historical block hash at `0xdbb`; clock skew was approximately 1.1 seconds. No reset, new contract deployment, developer-wallet shortcut or provider call occurred. `/mint?handle=karpathy` returned 200 and the browser shows the prefilled handle, **Connect wallet**, and disabled **Mint & reveal**.

Next gate: the user reconnects/verifies the wallet and chooses **Mint & reveal** once. This consumes the already-approved staged attempt. Only after a successful saved assessment can the user approve the Anvil mint. E10 and E11 live-provider validation remain incomplete; this section records staging, not successful research or minting.
