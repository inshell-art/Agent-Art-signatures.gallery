[profile.default]
src = "src"
test = "test"
out = "out"
cache_path = "cache"
libs = ["../node_modules"]
remappings = ["@openzeppelin/contracts/=../node_modules/@openzeppelin/contracts/"]
solc = "bin/solc"
evm_version = "prague"
optimizer = true
optimizer_runs = 200

[fuzz]
runs = 512
