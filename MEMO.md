- We need to ask Grok on X to resolve the capitalization of the handle the user entered to its canonical spelling. This runs in the Grok chat, not on our site.
- Next-agent handoff (2026-09-17): the only unfinished item in the current design pass is the homepage slogan wording. The product now lets anyone mint any available handle, rather than only claiming their own X identity. The live literal is still `What_shape_do_you_go_by?`. Proposed, not approved or implemented: `Whose_shape_will_you_reveal?` (recommended), `What_shape_does_a_handle_take?`, or `Any_handle._One_signature.`. Agree on the wording with the user before recapturing artwork. The supporting line remains “Any X handle. One minted signature.”
- Preserve the restored animation: `sg-slogan-mbti-1.2.3`, eight shapes in order ISTJ, ISFJ, INFJ, INTJ, ISTP, ISFP, INFP, INTP; 1000ms smooth fade followed by 1000ms hold, 16-second loop, beginning immediately with the first fade. The user tried paired 250ms ABAB and 125ms ABABABAB flashes and explicitly reverted both. Do not restore flashing. Keep the Ink hook question mark, literal hover/focus tooltip, no visible pause button, hover/focus/hidden/offscreen suspension, and static reduced-motion/no-JavaScript fallback.
- Slogan implementation: `src/brand/sloganMbtiHero.ts` owns presentation; `src/openMint/pages.ts` derives the accessible heading and tooltip from its manifest. The eight locked paths live in `src/brand/sloganMbtiFrames.ts`, generated offline by `scripts/capture-slogan-v2.py` using the verbatim v2.0.1 reference's long-text policy. A newly approved literal needs a deliberate recapture plus coordinated updates to `reference/algorithm-v2.0.1/slogan-lock.json`, `scripts/verify-slogan-v201-lock.mjs`, the relevant slogan tests and documentation. Do not merely change tooltip text, hand-edit paths, disable hash checks, or modify vendored upstream Python/JSON. `python3 scripts/capture-slogan-v2.py --emit` prints a candidate; `--check` verifies the checked-in capture. Keep historical v1 slogan files/locks and the actual signature renderer v2.0.0 unchanged. Public account-handle validation remains 1–15 characters.
- Keep the settled homepage layout while refining the slogan: desktop gallery net width 1024px, top navigation centers aligned to gallery edges, slogan max-width 56rem, hero top padding 7rem, responsive bottom padding, and mobile overflow protection. Relevant regression files include `src/openMint/homeGalleryLayout.test.ts`, `src/brand/sloganMbtiHero.test.ts`, `src/brand/sloganMbtiFrames.test.ts`, `tests/sloganV201Lock.test.ts`, and `src/openMint/pages.test.ts`. Current artwork navigation is `/p/<exact-case-handle>/variations`, captions are `@handle × MBTI`, and publisher footer links remain distinct from artwork-handle links.
- UI development at this handoff uses `http://127.0.0.1:3003/` in explicit fixture mode with no chain. Restart only when needed using `PORT=3003 OPEN_MINT_ORIGIN=http://127.0.0.1:3003 OPEN_MINT_DATA_DIR=.local/open-mint/preview-reveal-20260915 node --import tsx src/main.ts --fixture`. Check for the existing server first; shut it down gracefully rather than deleting its writer lock. Fixture gallery samples intentionally use production-style page copy without DEV overlays; they are not real mints. Do not reset local chain/data, rewrite saved artwork, call paid X/Grok services, or submit wallet transactions for a slogan task. Keep `.env.local` and `.local/` ignored.
- Checkpoint validation (2026-09-17): `OPEN_MINT_TEST_HTTP=1 npm run test:coverage` passes 3256 tests across 113 files, with 94.78% statements/lines, 90.38% branches and 97.89% functions; coverage thresholds are unchanged. Build, renderer locks, TypeScript checks and all 65 Solidity tests pass. The first sandboxed full test attempt hit loopback/process EPERM; the authorized HTTP-backed rerun passed. The restored slogan passed 641 browser timeline samples with zero mismatches or overflow. These checks use mocks/fixtures and do not establish a live paid-provider integration or public deployment.
- “Only slogan refinement” applies to this UI handoff, not production readiness. See `docs/open-mint.md` for existing production gates: public artifact publication, durable multi-instance storage/sessions/rate limits, paid-provider abuse controls, indexing/finality/recovery, deployment review and external security review; production startup remains blocked. New real preparations require both `XAI_API_KEY` and `OPEN_MINT_X_BEARER_TOKEN`; no live X/Grok verification was performed for this checkpoint. `INBOX.md` still records the older future X-account-ID link item. The earlier root `HANDOFF.md` is historical and contains superseded `/s/`, claim/OAuth and local runtime details; use this memo, README and `docs/open-mint.md` for the current state. The user requests ntfy completion notifications in this chat.
